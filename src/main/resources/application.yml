#xxl.job.admin.addresses: http://192.168.1.179:8080/xxl-job-admin
#xxl.job.executor.appname: xxl-job-executor-sample
server.port: 8449

test.value: test_${user.name}


spring:
  # ???????
  datasource:
    # ???????
    type: com.zaxxer.hikari.HikariDataSource
    # ????????????
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/test?rewriteBatchedStatements=true&allowMultiQueries=true&useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useAffectedRows=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
  redis:
    host: 192.168.0.200
    port: 6379
    password: netopstec
    database: 8
  data:
    elasticsearch:
      client:
        rest:
          uris: https://192.168.0.200:9200 # es的连接地址及端口号
          username: elastic
          password: elastic
          connection-timeout: 10000 # 连接超时时间，单位毫秒
          socket-timeout: 30000 # 套接字超时时间，单位毫秒
          max-conn-per-route: 100 # 每个路由的最大连接数
          max-conn-total: 200 # 总的最大连接
          use-ssl: true
mybatis-plus:
  global-config:
    db-config:
      id-type=auto:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

management.endpoints.web.exposure.include: *
management.metrics.export.influx.enabled: true
management.metrics.export.influx.uri: http://localhost:8086
management.metrics.export.influx.org: my-org
management.metrics.export.influx.bucket: my-bucket
management.metrics.export.influx.token: my-token
management.tracing.enabled: true

management.zipkin.enabled: true
management.zipkin.base-url: http://localhost:9411/
